C언어 시작
가장 어렵고 까다로운 언어
포인터로 인해 아주 어려워진다.
포인터로 인해 자료구조, 알고리즘, 메모리구조 등 다양한 장르 파생
포인터에 대해 일주일 동안 파악하는 것이 최종목표

C언어 특징 : 절대 재사용(객체)라는 개념이아니다. 선언하면 메모리에 할당댐(동적할당) 
파이썬과 다르게 사이즈(크기)를 지정해줘야 한다.(고정된 크기)

C언어는 visualstudio가 아니다. visualstudio는 컴파일러가 탑재된 툴일 뿐이다.
C언어의 컴파일러는 gcc와 g++.
visualstudio는 유료이기때문에 기업에서 자주 사용하지 않음

compiler는 프로그래밍언어를 기계어(실행파일)로 바꿔주는 것 gcc
c++ = g++
java = javac

ctrl+f5, f5는 되도록이면 사용하지 말것
f5 디버그모드는 개발자가 코드의 오류부분을 찾기위해 쓰는 모드

C언어의 장점은 포인터를 활용하여 메모리에 직접접근한다.
빠르다.
단점 포인터가 어렵고 프로그래머가 생각해야되는 변수들이 너무많다.
프로그램을 빠르게 작성하는데 어려움이 있다. - 알고리즘

#include<stdio.h>
void main();
printf("hello world");

stdio.h는 header파일
정의만 들어가 있음
구현X
c파일에 구현이 들어가있음(헤더파일과 동일한 이름으로)
stdio.c

#include<stdio.h>
void main();
	printf("1,2");
	print("5");

에러는 컴파일러가 잡아준다.

#include<stdio.h>

void main()
{
	printf("hello world");
}

운영체제가 다르면 리눅스 윈도우 똑같이 gcc가 있지만 똑같지 않다.
각각 실행파일도 호환되지 않는다.(윈도우용 맥용 리눅스용 나눠진까닭)

자료형
데이터를 담을 공간 = 메모리

연산은
1. 함수내에서 할 수 있음
2. 자료형이 있을 때 가능
3. 자료형이 다르면 안댐

사칙연산은 함수에서 사용한다.
같은 자료형만 사칙연산 가능하다.

c언어의 자료형
char, short, int, float, double, long

