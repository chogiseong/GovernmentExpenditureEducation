복습
포인터 : 주소를 통해 값을 찾아오는 연산 = *
포인터변수 : 주소 = 자료형*

double* a = &b; (여기서 double은 주소 double이기때문에 4바이트)

char a = 'a';
char b[50] = "ab"; //b는 b[0]의 주소
char* c = "ab"; //c는 주소

#터미널에서 한글이 한박자 늦게 찍히는 이유는 ASCII코드 이기 떄문이다.
c에는 스레드가 없다. 프로그램을 실행하는데로 실행됨



선행연구 혹은 관련연구
기본적으로 안드로이드 스튜디오, 자바 등에 대한 전반적 이해도와
ARcore, Unity 등 증강현실 엔진에 대한 이해도가 필요하며
엔진을 통한 심리적 안정감을 줄 캐릭터 모델링 및 직관성 있는 아이콘 디자인,
음성인식, 행동인식에 대한 학습 등의 기능은 팀원 중 영상처리 및 딥러닝 특강을
통해 경험이 있는 학부생이 있기때문에 충분히 구현 가능할 것이라고 생각합니다.

추가적으로 음성인식에 대한 연구, 심리치유에 관한 연구자료 학습 및
노인 고독사에 대한 접근, 연구 등의 자료를 학습계획서를 작성하여 학습 후
UI에 반영할 계획입니다.

void* 자료형이 없는 포인터.
type casting을 통해 자료를 바꿔줄 수 있음.

void main(){
	int a =0;
	int(*arr[5])(int, int) = {sum,sub,mul,dev}
	arr[0](1,3);
	arr[1](1,3);

	int p =0;
	scanf("%d", &p);
	arr[p](1,2);
}
등 함수를 순차적으로 사용가능(중요)

구조체
묶어주는것
함수를 제외한 전체를.

클래스 = 변수 + 메소드(함수
구조체 = 변수

링크드리스트 : 크기에 제한이 없다.
배열 : 같은자료형, 연속된주소값, 크기
링크드리스트(연결리스트) 데이터를 연결해서 무제한으로 넣음
데이터 뒤에 자기자신을 가르키는 포인터를 만듦
데이터를 struct(구조체)로 
구조체끼리 연결도 가능
삽입삭제가 쉽지만 사이즈가 무제한이다 데이터찾는속도가 느리다.
포인터하나만틀려도 다음 리스트가 날아가기때문에 그 문제를 보완하기 위한 더블링크드리스트. 환형링크드리스트가 있다.

typedef


