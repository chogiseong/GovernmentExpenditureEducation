재고관리 프로그램에 간단하게 인공지능을 넣어보자.
포트폴리오 작성 가능.

복습

클래스변수 - 클래스가 만들어질때 메모리가 할당됨(클래스이름, 변수이름)
인스턴스변수 - 객체마다 변수가 달라짐(self, 변수이름) 

오버로딩 - 함수이름이 같고 매게변수가 다를 때 다른함수로 인식되는 현상
오버라이딩 - 상속 함수이름, 매게변수도 같을 때 자식 클래스의 함수로 덮어쓰기 되는 현상

모듈 불러오기
패키지
도트를사용해 파이썬모듈을 계층적으로 관리할 수 있게해준다.

모듈 파일추가 (현재디렉토리 하위디렉토리)
패키지 폴더 __init__.py 존재해야 인식 import package이름.module이름

예외처리 내가아는 에러를 에러가 아니게 하기위해 사용
try/finally/except

thread
	import threading
	threading.thread(target)
	t.start()



////////////////////////////////////////////////////////////////////////////////////////////
테스트 정답

기본적인 문제들

파이썬 스트링 포맷
1. +
2. .format
3. %
4. f'{}

리스트 [] 수정가능
튜플 () 수정불가
딕셔너리 key, value

오버로딩
이름같고 매개변수의갯수 자료형이 다른함수
오버라이딩
상속에서만 사용되며 이름매개변수갯수자료형이 같아서 자식클래스의 함수로 덮어쓰기됨

레이스컨디션
하나의자원을 두개이상쓰레드가 접근하면서 생기는문제
