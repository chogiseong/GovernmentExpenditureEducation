정렬
순차정렬
삽입정렬
버블정렬
퀵정렬
중 순차, 버블은 수행가능해야

call by value = 값에 의한 참조

call by reference = 주소에 의한 참조
포인터 = 주소

/////////////////////////////////////////////////////////////
CLASS
변수 + 메소드(함수)
class는 메모리에 올라가지 않는다.
객체는 메모리에 올라간 class.
int, char, short, float 등이 변수(책에선 자료형) 나머진 class(참조형(배열리스트클래스))
class 안에 pass적어주면 빈클래스 완성

메모리에 올라가는 시점에서 수행 - 초기자
메모리에서 제거되면 하고싶다 - 소멸자(자바는 가비지컬렉터가 존재하기때문에 소멸자가없음)

인스턴스변수와 클래스변수의 차이

///////////////////////////////////////////////////////////////
복습
클래스는 변수 메소드 선언만+생성자(초기자)+(소멸자)
객체(인스턴스):메모리에클래스를올린다. = 메모리에 클래스를 할당했다.
mA=A()
객체이름, 생성자(클래스이름의함수)
클래스변수 - 클래스내부에 선언되어있는변수
인스턴스변수 - 객체가 살아있을때만유지
자료형, 참조형

###########if of while class def 이 다섯가지만 쓸 수 있으면 OK##############

생성자(초기자)

메소드 오버로딩
함수의 이름은 동일하고
매개변수의 갯수나 매개변수의 자료형이 다를때 다른함수로 인식되는 것

메소드 오버라이딩
상속에서만 가능하며 함수의
이름과 매개변수의 갯수 및 자료형이 같은 함수로 자식 클래스의 함수로 덮어쓰는 방법이다.

상속

보통 상속은 기존 클래스를 변경하지 않고 기능을 추가하거나 기존기능을 변경하려고 

상속을 사용하는 이유
공통 특징을 정의해주기 위해 부모클래스로 미리 선언

모듈 = 파일 .py
패키지 = 폴더 
파일단위의 함수, 클래스, 변수의 모음이라고 생각하면 된다.

예외처리, 쓰레드에 대해 찾아보기
재고관리 프로그램 만들기 디자인 해오기

창고클래스
입출력 클래스
입출력 함수
창고관리자
if 창고수정/ 로그확인

for문----------------첫날, 다음날 순으로 계속 증가
창고액셀파일불러오기

창고클래스 저장 수정/입출력 함수 사용
if문 활용하여 재고 소진 및 재고 추가 시 기능

로그텍스트파일 저장
----------------------


