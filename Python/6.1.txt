1개월
자바 c 파이썬
2개월
데베 웹프 자료구조알고리즘
3개월
인공지능 iot
4개월
프로젝트 전문가 멘토링
5개월
프로젝트 개발과 보완
6개월
포트폴리오 취업지원
////////////////////////////////////
topcit 2차 시험 응시
1000점 만점
400점 이상 시 포트폴리오 활요 가능
난이도 정보처리기사 이상
////////////////////////////////////
ncs등급
질문 하기
1.자바
2.c
3.파이썬
4.c++
5.c#
c와 java의 차이
include와 import의 차이
비주얼과 이클립스의 차이
객체지향과 순차지향의 차이
class의 존재 차이
객체 = 메모리할당
순차지향은 전부 메모리 할당
객체지향은 필요한 것만 할당
////////////////////////////////////
언어의 역사
c-c++, java -c#
모듈화의 필요성으로 인해 파생(c - c++)
포인터할당 문제로 인해 파생 + class (c - java)
java는 포인터가 없으며 jvm 다른 환경에서도 사용하기 위해 개발 느리지만 호환성이 좋다.
c#은 java와 c++ 장점을 합쳐 호환성이 좋다.
////////////////////////////////////
python
c의 특징을 들고옴
python 특징
1.
인터프리터 
한 줄씩 해석 (실행할 때마다 속도가 일정하게 소요)
하드웨어 기술이 좋아짐에 따라 암흑기가 찾아옴
자료형 없이 인터프리터가 판단
소스 수정 시 바로 적용 가능
한 줄씩 읽어 내려가기 때문에 느림

컴파일러
프로그래밍언어를 기계어인 어셈블리어로 변환
전체 소스 해석 (처음 빌드 시 시간 많이 소요, 다음 부턴 그냥 실행)

2. 
라이브러리의 수 (인공지능)

python 장점
인터프리터 언어이기 때문에 즉각 표현 가능하다. ex) javascript 
(os에 따라 메모리구조가 다르다. 따라서 os에 맞는 인터프리터가 필요하다.)
객체지향적 언어이다.
대화형 언어이다.
플랫폼(os)에 독립적이다. (jvm과 비슷)

static type
데이터 사이즈를 정해주는 자료형을 지정해야 한다. 
컴파일 시 자료형이 맞지 안으면 컴파일 에러를 발생한다.
int a = 3;
a 라는 공간에 3을 넣겠다. 대입연산자

dynamic type
자료형을 지정하지 않고 자유롭게 사용하여 편리하게 개발할 수 있다.
느리고 runtime 시 type error가 발생할 우려가 있다. (실행 중 에러가 발생할 수 있다.)

프로그램을 구성하는 기본 3가지 요소


python path 는 환경변수 중 하나

list는 배열과 비슷한 특징을 갖고 있다. (데이터를 묶어줌)

space와 tap은 다르다.

c언어의 자료형
컴퓨터는 2진수밖에 모름, 2진수 8진수 16진수 등이 있다.
2진수 한계에서 1을 더하면 부호비트가 바뀌기때문에 부호가 바뀌게된다.

naming rule
글자, _ 가능
대소문자 구분 가능
한글 가능
숫자로 시작할 수 없다.

data type - 정수 실수 논리형
Boolean 형의 True False 는 반드시 대문자로 작성
정수는 int 실수는 double이 기본 자료형이다. float 아님

data type - string
문자열 곱 가능
문자열 길이 구하기 len(a)

data type - string formating
%s string
%c character
%d decimal
%f 부동소수
%o 8진수
%x 16진수
%% 

data type - 관련 내장 함수