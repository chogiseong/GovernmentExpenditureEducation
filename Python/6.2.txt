복습
클래스 안에 상속, 오버라이딩, (메소드)오버로딩 등 속성이 많다.
클래스 : 메모리에 할당되지 않고 함수 변수 등이 정의만 된 상태 (껍데기, 붕어빵 틀)
객체 : 메모리에 올라가있는 실체 (알맹이, 붕어빵)
객체 지향 : 객체 사용을 권장하는 방법 (c언어가 아닌 언어는 모두 객체 지향 언어 이다.)

c언어와 java, python의 다른 점
java : 클래스, jvm
python : 클래스, 인터프리터

python을 인공지능에 사용하는 이유
라이브러리가 많다.
구글이 파이썬을 선택했기 때문에 텐서플로(라이브러리)가 개발됨
python은 라이브러리가 내부적으로 기본 기능이 들어가 있다.

string format
문자열 조합법
1. 문자열더하기(형변환해줘야함)
2. "%s" % "문자열"
3. "{0}" format("안녕")
4. f"{name}" //python 3.6부터 지원

count
strip
split 등 내장함수가 있음

한글은 2byte, 16bit를 사용
c는 문자를 1byte로 표현해야 한다고 생각. ASCII코드 방식 사용
python은 2byte 체계인 유니코드 방식 사용(자바도 사용) 따라서 성능차이 두배

data type - list
한꺼번에 모아서 사용
대괄호로 사용
배열 = '같은 자료형'을 연속된 메모리로 저장한 것
자료형은 메모리에 얼마나 저장해야 할지 선언해줘야하기 때문에 필요
배열은 같은 자료형이기 때문에 한꺼번에 처리 가능

python은 배열이 없다.
아무거나 다 저장할 수 있는 list를 만들었다.(아무거나 넣기때문에 편함)

언어(c,java)
알고리즘 - 자료구조 - 배열, 리스트, 스택, 큐, 트리, 그래프(큐, 리스트, 스택 주용)
이중 리스트를 파이썬이 가져옴

리스트는 데이터의 집합
인덱스를 통해 데이터를 가져온다. 인덱스는 0번부터 시작.
ex)리스트 데이터 = [1,2,3,4,5]

list sort
sort
reverse
index(3) 3의 인덱스가 뭔지 알 수 있음
추가
insert(0,4) 0번의 인덱스에 4를 넣어라 (인덱스와 데이터 포함해야함)
extend 데이터만 붙임(내부속성을,데이터만 붙임)
append 그대로 붙임(전체를 붙임)
##변수명으로는 예약어를 사용하면 안된다.

삭제
remove 자료형이 같아야함(데이터 기준)
pop 맨뒤의 요소를 돌려주고 삭제(지정도 가능)

python list - tuple
고치지 못하는 리스트의 데이터 소괄호로 선언
tuple을 리스트로 바꾼 후 추가 후 다시 tuple로 변환하는 작업으로 수정가능

java의 hash map 같은 딕셔너리가 있음
test={key:value}

리스트[] 추가삭제변경가능
튜플() 변경불가
딕셔너리{} key:value

set명령
중복을 허용하지 않는다
순서가 없다

조건부 표현식
score=50
message = "success" if score >=60 else "fail"

while
if 조건식 :
	//////
else :
	//////
while 조건식 :
	//////
반복문은 반드시 탈출조건이 필요하다.

python for
리스트를 갖고오는 역할
while문과 동일하나 for문 범위지정으로 많이 사용